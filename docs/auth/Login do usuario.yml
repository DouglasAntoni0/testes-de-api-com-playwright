info:
  name: Login do usuario
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3333/auth/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
